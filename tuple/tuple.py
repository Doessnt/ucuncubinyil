import random
######################TUPLE#######################
#Listelerden farkı hiçbir şekilde içeriği değişemez.
"""
tuplelist = (1, 2, 3, 4, 5, 6, "A", "B", "C", "D")

print(tuplelist)
print(tuplelist.index("A")) # index sayısını verir
print(tuplelist.count("B")) # listenin içinde kaç adet olduğunu verir
print(len(tuplelist))
print(tuplelist[7])


mytuple = (4, 2, 3,[6,4]) # tuple içine liste eklme

mytuple[1] = 9 # Hata verir.
mytuple[3][0] = 9 # Hata vermez.

my_tuple = ('a', 'b', 'c', 'd', 'e', 'f', 'g')

print('n' not in my_tuple)
"""


































































































































